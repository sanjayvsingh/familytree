# Exclude GEDCOM files to protect personal family data
/gedcom/*.ged
/gedcom/*.GED
/gedcom/*.gedcom
/gedcom/*.GEDCOM

# Parser cache — generated at runtime, never commit
/cache/

# Auth runtime data and secrets — never commit
/auth/
/config.php
